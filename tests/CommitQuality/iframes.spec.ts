import { test, expect } from '../../fixtures/CommitQuality/base.ts'

test.describe('iFrames', () => {

  test.beforeEach(async ({ iframesPage }) => {
    await iframesPage.goto()
  })

  test('', async ({ iframesPage }) => {
    
  })
})