import { test, expect } from '../../fixtures/CommitQuality/base.ts'

test.describe('Dynamic Text', () => {

  test.beforeEach(async ({ dynamicTextPage }) => {
    await dynamicTextPage.goto()
  })

  test('', async ({ dynamicTextPage }) => {
    
  })
})