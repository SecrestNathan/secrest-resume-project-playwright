import { test, expect } from '../../fixtures/CommitQuality/base.ts'

test.describe('Drag and Drop', () => {

  test.beforeEach(async ({ dragAndDropPage }) => {
    await dragAndDropPage.goto()
  })

  test('', async ({ dragAndDropPage }) => {
    
  })
})