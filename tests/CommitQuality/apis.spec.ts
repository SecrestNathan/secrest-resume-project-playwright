import { test, expect } from '../../fixtures/CommitQuality/base.ts'

test.describe('APIs', () => {

  test.beforeEach(async ({ apisPage }) => {
    await apisPage.goto()
  })

  test('', async ({ apisPage }) => {
    
  })
})