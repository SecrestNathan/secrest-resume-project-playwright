import { test, expect } from '../../fixtures/CommitQuality/base.ts'

test.describe('Popups', () => {

  test.beforeEach(async ({ timeTestingPage }) => {
    await timeTestingPage.goto()
  })

  test('', async ({ timeTestingPage }) => {
    
  })
})