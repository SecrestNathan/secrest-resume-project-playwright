import { test, expect } from '../../fixtures/CommitQuality/base.ts'

test.describe('Mock Data Layer', () => {

  test.beforeEach(async ({ mockDataLayerPage }) => {
    await mockDataLayerPage.goto()
  })

  test('', async ({ mockDataLayerPage }) => {
    
  })
})